import localization from 'react-native-localization';

const locals = new localization({
  en:{
    label_username:'Username',
    label_password:'Password',
    label_login:'Login'
  }
});

export default locals;
